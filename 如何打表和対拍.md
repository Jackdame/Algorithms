#算法 

# 如何打表和対拍

## 一、打表（Precomputation / Table Making）

### 1. 什么是打表

打表，是指在算法竞赛中，**预先计算出一些结果并存储下来**，在实际运行时直接查表获取答案，而非实时计算。与之相对的是在本地暴力枚举所有可能的输入，计算出答案后**直接将结果数组硬编码进代码**提交。

### 2. 打表的常见形式

#### 2.1 预处理数组（最常见的打表）

在程序启动时一次性预处理出所需数据，后续直接使用。比如预处理素数表、阶乘、前缀和等。

```cpp
// 预处理素数筛
const int N = 1e6 + 5;
bool is_prime[N];
int primes[N], cnt;

void init() {
    fill(is_prime, is_prime + N, true);
    is_prime[0] = is_prime[1] = false;
    for (int i = 2; i < N; i++) {
        if (is_prime[i]) primes[cnt++] = i;
        for (int j = 0; j < cnt && i * primes[j] < N; j++) {
            is_prime[i * primes[j]] = false;
            if (i % primes[j] == 0) break;
        }
    }
}
```

#### 2.2 本地暴力枚举后硬编码（真正的"打表提交"）

当**输入范围非常有限**（比如只有几十或几百种可能）时，可以在本地写一个暴力程序枚举所有输入，算出所有答案，然后直接把答案数组复制到提交代码中。

**适用场景**：输入范围极小（如 n ≤ 50 或更小），且题目逻辑复杂难以直接写出高效解法。

```cpp
// 本地暴力打表程序（在本地运行，不是提交代码）
#include <bits/stdc++.h>
using namespace std;

int brute(int n) {
    // 暴力计算答案
    int res = 0;
    for (int i = 0; i < (1 << n); i++) {
        // 检查并更新答案
    }
    return res;
}

int main() {
    // 生成答案表
    cout << "int ans[] = {";
    for (int i = 0; i <= 20; i++) {
        if (i) cout << ", ";
        cout << brute(i);
    }
    cout << "};" << endl;
    return 0;
}
```

```cpp
// 最终提交的代码
int ans[] = {0, 1, 3, 7, 15, 31, 63, 127, 255, 511, 1023, 2047, 4095, 8191, 16383, 32767, 65535, 131071, 262143, 524287, 1048575};

int main() {
    int n;
    cin >> n;
    cout << ans[n] << endl;
    return 0;
}
```

#### 2.3 利用数学规律打表

有些题目有循环节或周期性特征，可以预处理一个周期内的值，然后用 **模运算** 将任意输入映射到表中。

```cpp
// 斐波那契数列模 1000 的循环节（皮萨诺周期）
int fib_mod[1500]; // 周期 ≤ 1500

void init() {
    fib_mod[0] = 0, fib_mod[1] = 1;
    for (int i = 2; i < 1500; i++) {
        fib_mod[i] = (fib_mod[i-1] + fib_mod[i-2]) % 1000;
    }
}

int query(long long n) {
    return fib_mod[n % 1500];
}
```

### 3. 打表的优缺点

| 优点 | 缺点 |
|------|------|
| 运行速度极快，O(1) 或 O(log n) 查询 | 代码可能很长（硬编码数组） |
| 避免复杂的实时计算逻辑 | 受代码长度限制（CF/AtCoder 通常 ≤ 64KB） |
| 适用于规律难找但范围小的题 | 打表程序本身需保证正确 |
| 减少运行时错误风险 | 无法处理范围过大的输入 |

### 4. 打表的决策流程

```
输入范围是否极小（≤ 几十或几百）？
  ├── 是 → 暴力枚举所有输入，硬编码答案数组
  └── 否 → 是否有预处理加速的空间？
       ├── 是 → 编写 init() 预处理，运行时查表
       └── 否 → 老老实实写正解
```

---

## 二、対拍（对拍，Stress Testing）

> "対" 是 "对" 的异体字/日本新字体写法，此处中二一下沿用这个写法。。。。hyw

### 1. 什么是对拍

对拍，是指**用多个程序（通常是一个暴力但正确的版本和一个优化后的版本）在大量随机数据上对比输出结果**，以验证优化版本的正确性。

### 2. 为什么要对拍

- 你的正解可能隐藏着 bug，小样例测不出来
- 暴力程序虽然慢，但逻辑简单、不易写错
- 随机生成大量数据，覆盖各种边界情况
- 自动化运行，省去人工构造数据的精力

### 3. 对拍的三个组成部分

| 程序 | 说明 |
|------|------|
| `brute.cpp` | 暴力正确解法（保证正确，不在乎效率） |
| `solve.cpp` | 优化后的待验证解法（你准备提交的版本） |
| `gen.py` / `gen.cpp` | 随机数据生成器 |
| `对拍脚本 (Windows .bat / Linux .sh)` | 自动循环运行并比较输出 |

### 4. 对拍完整示例（C++ 版）

#### Step 1: 编写暴力程序 `brute.cpp`

以"给定 n 个数，求和"为例（实际比赛中请换成你的题目逻辑）：

```cpp
// brute.cpp - 暴力正确解法
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    int sum = 0;
    for (int i = 0; i < n; i++) sum += a[i];
    cout << sum << endl;
    return 0;
}
```

#### Step 2: 编写待验证程序 `solve.cpp`

```cpp
// solve.cpp - 优化后的待验证解法
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    vector<int> a(n);
    int sum = 0;
    for (int i = 0; i < n; i++) {
        cin >> a[i];
        sum += a[i];
    }
    // 假设我手滑写了个 bug：故意少加一个数
    // 对拍就能立刻发现！
    cout << sum << endl;
    return 0;
}
```

#### Step 3: 编写数据生成器 `gen.py`

```python
import random
import sys

# 用法: python gen.py > input.txt
n = random.randint(1, 1000)
print(n)
# 生成 n 个随机整数
a = [random.randint(-1000, 1000) for _ in range(n)]
print(' '.join(map(str, a)))
```

或者 C++ 版 `gen.cpp`：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
    srand(time(0));
    // 可接受命令行参数作为随机种子，便于复现错误
    if (argc > 1) srand(atoi(argv[1]));

    int n = rand() % 1000 + 1;
    cout << n << endl;
    for (int i = 0; i < n; i++) {
        cout << rand() % 2001 - 1000 << " \n"[i == n - 1];
    }
    return 0;
}
```

#### Step 4: 编写对拍脚本

**Windows 批处理文件 `对拍.bat`**：

```bat
@echo off
chcp 65001 >nul
set cnt=0

:loop
set /a cnt+=1
echo 测试 #%cnt%

python gen.py > input.txt
brute.exe < input.txt > brute_output.txt
solve.exe < input.txt > solve_output.txt

fc brute_output.txt solve_output.txt > nul
if errorlevel 1 (
    echo [x] 找到差异！输入如下：
    type input.txt
    echo.
    echo 暴力输出：
    type brute_output.txt
    echo 你的输出：
    type solve_output.txt
    pause
    goto loop
)
echo [v] 通过
goto loop
```

**Linux / macOS Shell 脚本 `对拍.sh`**：

```bash
#!/bin/bash
cnt=0
while true; do
    cnt=$((cnt + 1))
    echo "测试 #$cnt"

    python3 gen.py > input.txt
    ./brute < input.txt > brute_output.txt
    ./solve < input.txt > solve_output.txt

    if ! diff -q brute_output.txt solve_output.txt > /dev/null; then
        echo "❌ 找到差异！输入如下："
        cat input.txt
        echo ""
        echo "暴力输出："
        cat brute_output.txt
        echo "你的输出："
        cat solve_output.txt
        exit 1
    fi
    echo "✅ 通过"
done
```

### 5. 对拍的进阶技巧

#### 5.1 使用 `system()` 统一编译和运行

在暴力程序或数据生成器中加入编译命令，避免手动编译：

```cpp
// 可以在主对拍程序里编译
system("g++ brute.cpp -o brute -O2");
system("g++ solve.cpp -o solve -O2");
system("g++ gen.cpp -o gen -O2");
```

#### 5.2 输出差异详情

除了简单的 diff，还可以输出具体是哪一行、哪一位不同：

```bat
fc brute_output.txt solve_output.txt
if errorlevel 1 (
    echo [x] 发现差异！
    pause
    goto loop
)
```

或者用 `comp` 命令（Windows）查看具体字节差异。

#### 5.3 定制随机数据

好用的随机生成策略：

- **小数据**：n 设为 1~10，便于手动验证
- **大数据**：n 接近数据范围上限，检查性能和越界
- **边界值**：n = 0, 1, 最大值；数值 = INT_MIN, INT_MAX
- **特殊结构**：链式图、完全图、升序/降序数组、全相同元素

```cpp
// gen.cpp - 更精细的数据生成
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
    int seed = argc > 1 ? atoi(argv[1]) : time(0);
    srand(seed);

    // 随机选择数据规模：小规模（便于调试）或大规模（检查性能）
    int type = rand() % 2;
    int n;
    if (type == 0) {
        n = rand() % 10 + 1;        // 小数据
    } else {
        n = rand() % 100000 + 1;    // 大数据
    }
    cout << n << endl;

    for (int i = 0; i < n; i++) {
        // 偶尔生成边界值
        if (rand() % 20 == 0) cout << INT_MAX << " ";
        else if (rand() % 20 == 1) cout << INT_MIN << " ";
        else cout << rand() % 2001 - 1000 << " ";
    }
    cout << endl;

    // 将种子输出到 stderr，便于重现错误
    cerr << "seed = " << seed << endl;
    return 0;
}
```

#### 5.4 限制对拍运行时间

长时间运行的对拍脚本可以通过计数退出：

```bat
@echo off
chcp 65001 >nul
set cnt=0
:loop
set /a cnt+=1
if %cnt% gtr 10000 (
    echo 已通过 10000 组测试，未发现错误！
    exit /b 0
)
echo 测试 #%cnt%
rem ... 其余部分同上
```

#### 5.5 并行对拍

对于大型对拍，可以使用多进程并发运行，利用 CPU 多核加速。Linux 下可以用 `xargs -P` 或 `parallel`；Windows 下可以用 PowerShell 的 `ForEach-Object -Parallel`。

### 6. 对拍常见问题

| 问题 | 解决方法 |
|------|----------|
| 暴力程序太慢，跑大数据超时 | 控制随机数据规模，大数据只测正解 |
| 数据生成器也需保证正确性 | 用简单逻辑生成，或者从已知正确数据中抽样 |
| 浮点数比较要容差 | 用 `diff` 不如用专门的比较器（如 `spj.cpp`） |
| Windows 下编译后 .exe 未更新 | 每次对拍前 `g++ solve.cpp -o solve.exe` 重新编译 |
| 对拍卡死（死循环） | 设置最大运行轮次或者超时机制 |

### 7. 浮点数对拍的特殊处理

对于浮点数输出，不能直接用 `diff` 或 `fc` 判等，需要编写一个**比较器**：

```cpp
// spj.cpp - 浮点数比较器
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
    ifstream brute(argv[1]);   // 暴力输出文件
    ifstream solve(argv[2]);  // 待验证输出文件
    double a, b;
    int line = 0;
    while (brute >> a) {
        if (!(solve >> b)) {
            cout << "[x] 行数不一致，暴力有更多输出" << endl;
            return 1;
        }
        line++;
        if (fabs(a - b) > 1e-6 && fabs(a - b) / max(1.0, fabs(a)) > 1e-6) {
            cout << "[x] 第 " << line << " 行: 暴力=" << a << " 你的=" << b << endl;
            return 1;
        }
    }
    if (solve >> b) {
        cout << "[x] 行数不一致，你的有更多输出" << endl;
        return 1;
    }
    cout << "[v] 通过" << endl;
    return 0;
}
```

### 8. 对拍的最佳实践总结

```
┌─────────────────────────────┐
│  发现 WA (Wrong Answer)    │
└──────────┬──────────────────┘
           ▼
┌─────────────────────────────┐
│  先手动构造小数据跑一遍     │
│  用小样例人工验证逻辑       │
└──────────┬──────────────────┘
           ▼
┌─────────────────────────────┐
│  写暴力程序（朴素正确版本） │
│  写数据生成器（含边界数据） │
│  写对拍脚本                 │
└──────────┬──────────────────┘
           ▼
┌─────────────────────────────┐
│  跑对拍，找到反例           │
│  根据反例调试优化版本       │
└──────────┬──────────────────┘
           ▼
┌─────────────────────────────┐
│  修完 bug，继续对拍         │
│  通过 1000+ 组随机数据      │
└──────────┬──────────────────┘
           ▼
┌─────────────────────────────┐
│        提交！[v]             │
└─────────────────────────────┘
```

---

## 三、总结

- **打表**解决的是"怎么获得答案"的问题 —— 空间换时间，适合输入范围小或可预处理的场景。
- **对拍**解决的是"怎么确认答案对了"的问题 —— 暴力验证正解，适合在不确定正解是否正确的调试阶段。
- 两者结合起来，可以大幅提升算法竞赛中的解题效率和正确率。
- 比赛中遇到 WA 但一时找不到 bug，立刻上对拍；遇到小范围数据，立刻考虑打表。

> 记住：**暴力出奇迹，打表进省一，对拍保 AC。**
